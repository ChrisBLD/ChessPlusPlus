#include <iostream>
#include "Engine.h"

int main()
{
    //Declare an instance of Engine
    Engine engine;

    //Start the Engine
    engine.run();

    //Quit in the usual way when the engine is topped
    return 0;
}
